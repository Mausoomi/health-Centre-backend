# HealthCenter Backend

This is the backend API service for the HealthCenter App, built with Node.js, Express, TypeScript, and MongoDB.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Configure `.env` based on `.env.example`.
3. Run the development server:
   ```bash
   npm run dev
   ```
